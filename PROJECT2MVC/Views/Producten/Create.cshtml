@model PROJECT2MVC.Models.Product

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Product</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.ProductCode)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.ProductCode)
            @Html.ValidationMessageFor(model => model.ProductCode)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.Omschrijving)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Omschrijving)
            @Html.ValidationMessageFor(model => model.Omschrijving)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.Eenheid)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Eenheid)
            @Html.ValidationMessageFor(model => model.Eenheid)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.PrijsPerEenheid)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.PrijsPerEenheid)
            @Html.ValidationMessageFor(model => model.PrijsPerEenheid)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
